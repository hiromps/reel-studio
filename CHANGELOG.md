# 変更履歴

更新のしかたは [README の「更新のしかた」](README.md#更新のしかた) を見てください（`npm run update`、または
Settings の「版と更新」から）。**案件データ・設定・人格はリポジトリの外**（`~/.reel-studio/` と
データフォルダ）にあるので、更新で失われることはありません。

---

## 0.2.0

### 追加

**素材の切り出し（アスペクト比を変えない拡大・位置）**

出来上がりは常に 9:16 のまま、枠の中で「どこを、どれだけ寄って見せるか」を決められます。
枠を動かすのではなく中の画を動かす作りなので、比率は崩れません。

- ピンチ・ドラッグ・ホイール・スライダー（1.00〜3.00×）。三分割の目安線つき
- 寄ったときの実質解像度を出すので、寄せすぎが分かります
- 素材（Materials・選別モード）で決めると、構成を組んだときにカットへ引き継がれます。
  Timeline の Inspector でカットごとに調整することもできます
  （既にある `cuts.json` には「素材の切り出しを取り込む」で反映）
- 当て方はレンダー用のエンジンと画面で同じ関数なので、**編集中に見えているものがそのまま書き出されます**

**選別モードで素材を編集できるようにした**

素材を 1 本ずつ大きく見て「必要／不要」を決めながら、その場で

- 使える区間（トリミング）を帯の上で掴んで決める。区間が無い素材は帯を掴んだ瞬間に
  「見せ場（best）」の区間ができます
- 決めた区間だけを繰り返し再生して確かめる
- 種別・画角・被写体を直す（Materials に戻らずに済みます）

キーボード: `→`/`K` 必要、`←`/`X` 不要、`H` フック候補、`I`/`O` 区間の頭／尻、`Z` 枠を大きく。

**顔モザイク（deface）**

店員さんや他のお客さんの顔にモザイクをかけられます。Settings の「顔モザイク（deface）」から
専用の venv に導入し、Materials でクリップを選んで適用・取り消しができます。

**版と更新**

Settings の「版と更新」で更新の有無と入る変更が分かり、そのまま更新できます。
`npm run update`（git pull → 依存の導入 → ビルド）でも同じことができます。

### 直したもの

- **動画がブラウザによっては再生できなかった** — `playsInline` の指定が無く、
  iOS 系ではインライン再生そのものができていませんでした
- **プレビューが画面に追従しない** — Materials の一覧は素材の本数ぶん縦に長いのに、
  右のプレビューが行の先頭に固定されていたため、下の方のクリップを選ぶたびに
  上へ戻る必要がありました
- **狭いウィンドウで横にはみ出す** — Materials・Render・設定の固定幅レイアウトを畳み、
  タブを折り返すようにしました

### 変わったこと（更新後に起きること）

- **エンジン（テロップ描画）が変わりました。** 既存案件は「エンジン差分あり」になり、
  次のレンダーで自動的に揃います（すぐ揃えるなら Projects の「エンジン同期」）
- `catalog.json` / `cuts.json` / `settings.json` への追加はすべて任意項目なので、
  **今までのファイルはそのまま読めます**（古い版に戻しても壊れません）

---

## 0.1.0

最初の公開版。素材のカタログ化・タグ付け・構成プラン（F0〜F7）・台本からの組み立て・
Timeline 編集・検証・音声生成（Fish Audio）・レンダー（Remotion）・合成・納品。
