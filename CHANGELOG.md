# 変更履歴

更新のしかたは [README の「更新のしかた」](README.md#更新のしかた) を見てください（`npm run update`、または
Settings の「版と更新」から）。**案件データ・設定・人格はリポジトリの外**（`~/.reel-studio/` と
データフォルダ）にあるので、更新で失われることはありません。

---

## 未リリース

### 追加

**ナレーション音声に映像の尺を合わせる（0.75〜0.8 秒のカットに刻み直す）**
「台本から組み立てる」→「音声を生成」のあと、Fish Audio の実測尺は台本の想定とずれる（同じ文でも 2 倍ばらつく）ので、
組み立て直後の構成は音声より長かったり短かったりしました。これまでは narration の `at` を後ろにずらすか、カットを
1 つずつ引き伸ばすしかなかったところを、Timeline のインスペクタ「動画全体」→ **「ナレーション音声に尺を合わせる」**
（CLI は `reel fit --project P`）で、**音声を正として映像の側を合わせ直せる**ようにしました。

- 各ナレーションブロックの映像区間をその wav の実測尺に揃え、中を **0.75〜0.8 秒（中央 0.775）のカットに刻み直す**。
  `at` はブロックの頭に置き直し、音声は作り直さない
- 素材はいまのカット列から取り、テロップ・バッジ・切り出しは元カットから引き継ぐ。**テロップは 1 つも落とさない**
  （1 カットしか無いテロップとフックは読めるよう 0.8 秒に伸ばす）。会話（字幕つき）とロック済みのカットは刻まない
- 同じ素材から複数取るときは素材の中で場所をずらす（ジャンプカット）。元の区間で足りなければ素材の残りを使い、
  それでも足りなければ同じ場面を重ねて使って注記する。効果音は相対位置で新しいブロックへ写す
- 結果（ブロックごとの 音声 → 映像 の秒数・カット数・注記）はインスペクタに出る。**Ctrl+Z で cuts と narration が一緒に戻る**。
  検証パネルの「ナレーション」タブにも、重なり・はみ出しがあるときに同じボタンが出る
- 音声が無いブロックがあると止まる（`reel fit --estimate` で文字数見積もりに切り替えられる）。`--dry` で書かずに結果だけ、
  `--min` / `--max` で刻みの範囲、`--lead` / `--tail` で先頭・末尾の余白。詳細は `docs/guide.md` の
  「ナレーション音声に尺を合わせる」。ロジックは `shared/fit.ts`（純粋・テストあり）

**店舗情報の裏取りで、店の Instagram を Smartgram の MCP 経由で直接読む**
キャプションを書く前の裏取り（`ai-facts`）は Instagram を最優先の出典にしているのに、Instagram のページは
ログイン壁で読めず、検索スニペット頼みになりがちでした。Settings に **「Instagram の情報取得（Smartgram MCP）」**
を置き、[Smartgram](https://app.smartgram.jp/) の MCP 用 API キーを入れると、裏で走らせる claude にその
MCP サーバーを渡して、登録済みアカウント経由で店の公開アカウント（プロフィール・最近の投稿）を直接読ませます。

- 許可するのは読むだけのツール（アカウント一覧・ユーザー検索・プロフィール・投稿・API 残量）。
  課金されるストーリー取得・動画ダウンロードや、投稿・フォローの操作は渡しません
- 鍵は Fish Audio と同じく `settings.json` に保存（画面には末尾 4 桁だけ。環境変数 `SMARTGRAM_MCP_KEY` が優先）。
  claude には引数やファイルではなく環境変数で渡します
- 「接続テスト」で鍵の有効性と、Smartgram に登録済みのアカウント一覧（有効／無効）が出ます。
  「実行アカウント」を空にしておくと AI が有効なものを選びます
- 鍵が無ければ従来どおり Web 検索だけで裏取りします。Render のキャプションカードに、どちらで読むかが出ます
- `reel settings show` に Instagram の行が増えました。詳細は `docs/guide.md` の「店舗情報の裏取り」

**バズ動画の型を写す（他の人の伸びたリールを渡すと、その型で自分の素材の動画を作る）**
Brief に **「バズ動画の型を写す」** を置きました。他の人が投稿して伸びたショート動画を選ぶと、
シーン検出・コンタクトシート・無音検出を作って Claude に読ませ、**型**（区間と秒数・カット数・テロップの型・
フックの掛け方・店名を明かす位置・締め方・保存させている情報・テンポの癖）を `reference.json` に言語化します。
そのあと **「この型で台本を作って組み立てる」** を押すと、参考と**同じ区間数・同じ秒数・同じカット数**で
`script.md` を書き、続けて「台本から組み立てる」が素材を割り当てて `cuts.json` と `narration.json` まで作ります。

- **写すのは構成・テンポ・テロップの型だけ。** 参考動画の映像・音声・文言そのものは使いません
  （プロンプトで禁じ、参考のテロップと同じ文言が残っていれば検算 `MIMIC_TELOP_COPIED` で指摘します）
- 区間の秒数は AI が返した値ではなく参考動画のものを強制します（`fitMimicToReference`）。E があれば台本を書きません
- 分析結果は画面で確認できます（尺・カット数・平均カット秒・声の割合、フック／リビール／締め、区間の表、
  カットの一覧、写すときの規則）。「別の案件の分析を使う」で、前に分析した型をそのまま別の店に当てられます
- スマホ（クラウドモード）からも動画を上げられます（素材と同じく Blob へ直接上げて PC が受け取る）。
  クラウドに上がるのは分析結果とコマだけで、動画本体は PC の `.studio/reference/` から出ません
- 受け付けるのは 3 分までの動画。取り消すとファイルを消す代わりに墓標を書き、クラウドにも「消した」が伝わります
- CLI は `reel ai reference --project P --file <動画>`（`--show` / `--from` / `--remove`）と `reel ai mimic --project P [--dry]`
- ジョブは `ai-reference` / `ai-mimic`。詳細は `docs/guide.md` の「バズ動画の型を写す」

**「⟳ 最新に」ボタン（PC で直したものをスマホへすぐ出す）**
PC でキャプションや構成を直しても、スマホ（クラウドモード）では古いままに見えることがありました。
上部バーに **「⟳ 最新に」** を置き、押すと **PC に「いま送って」と頼んでから読み直す**ようにしました。

- クラウド版では同期ジョブ（`sync`）を積み、PC が契約ファイル・サムネイル・軽量プレビュー・書き出しを送り返します。
  ワーカーの棚卸し（5 分ごと）を待たずに反映されます
- ローカル版では案件のファイルを読み直すだけ（この PC のファイルをそのまま見ているため）
- **未保存の編集は上書きしません**（読み直さずに知らせます）
- PC が繋がっていないときは「PC が起きたときに同期されます」と出し、ジョブは待機のまま残ります

**投稿し終えた案件を一覧から隠せる**
案件は増える一方で、投稿して編集の終わったものも一覧に残り続けていました。Projects の各行の
**「投稿済み（隠す）」** で、一覧と上部の案件選択から外せるようになりました。
**隠すだけで消えません**（案件フォルダ・素材・書き出し・契約ファイルには触りません）。

- 戻すときは見出しの「隠した案件も表示（N 件）」にチェック →「一覧に戻す」
- **スマホでは案件一覧を 1 案件＝1 枚のカードに積み直しました**。11 列の表を横スクロールさせていたので、
  右端の操作列（「開く」「投稿済み（隠す）」）まで指が届いていませんでした
- 開いている案件は、隠しても一覧に残ります（`（非表示）` と出る）。開いたまま消えて操作できなくなるのを防ぐため
- 「同じ素材から作る」の選択肢には隠した案件も出ます（投稿済みの案件から第 2 弾を作る場面が多いため）
- 状態は案件の `.studio/meta.json` に入り、**PC とスマホ（クラウドモード）で同期**されます。
  どちらで隠しても、どちらで戻しても、もう一方に伝わります
- `reel projects` は隠さず、slug に `※非表示` と付けるだけ（CLI は棚卸し用なので全部出します）

**自前のフォントを取り込んでテロップに使える**
これまでテロップは同梱の明朝（Noto Serif JP Bold）固定でしたが、Settings の「テロップのフォント」から
手持ちのフォント（ttf / otf / ttc / woff / woff2）を取り込んで使えるようになりました。
取り込んだものは見本つきで一覧に出るので、見た目を確かめてから選べます。

- 置き場は `~/.reel-studio/fonts/`（案件フォルダの外なので、更新でも案件の引っ越しでも消えません）
- Settings で選んだものが**これから作る動画**の既定。案件ごとに変えるときは Timeline の「動画全体 → フォント」
  （`cuts.json` の `font`。構成を作り直しても、案件で選んだフォントは戻りません）
- 選ぶと案件の `public/fonts/` へ自動で配られるので、**プレビューと書き出しが必ず同じ絵**になります
- フォントが見当たらないときは同梱の明朝で描き、レンダーの警告に出します（止まりません）
- スマホ（クラウドモード）からは選ぶだけできます。取り込み・削除は PC 側で（実体は PC にあります）
- テロップは太字前提なので、Bold / 太ゴシック・太明朝を入れると綺麗に出ます。
  **持ち込むフォントのライセンス（商用利用・埋め込みの可否）は利用者の責任で確認してください**

**PC で Reel Studio を起動すれば、スマホからも繋がる**
これまでクラウドモードでは、PC 側で `npm run worker` を別に起動しておかないとスマホから
「PC オフライン」のままでした。デスクトップのショートカット（ランチャー）がサーバーと一緒に
ワーカーも立ち上げるようにしたので、**いつもどおり起動するだけ**でスマホから使えます。
窓を閉じれば止まります（クラウド接続を設定していない場合は何も起きません）。

- 二重起動よけを入れたので、ショートカット・`npm run worker`・タスクスケジューラが重なっても
  走るのは 1 つだけ（錠は `~/.reel-studio/worker.lock`。落ちた後の錠は無視される）
- Reel Studio を開いていない間も繋げておきたい場合は、これまでどおりワーカーを常駐させる
  （`docs/cloud.md` の「Windows で常駐させる」）

**「ファイル名を最適化」ボタン（同じ素材を離れた位置で使っているとき）**
同じ素材を飛び飛びに参照しているとレンダーが不安定になるため検証が
`E SAME_SRC_NONCONSECUTIVE` で止めますが、これまでは自分で直す手立てが画面にありませんでした。
仕上げカード・レンダーカード・Timeline の検証パネルにボタンを出し、押すだけで
**中身が同じ別名のコピー**を作って参照を振り替えます（並び・テロップ・出来上がりの映像は変わりません）。

- 何件直るかを先に出し、レンダーの前に押せる。既に正しければボタンは出ない（何度押しても結果は同じ）
- 別名のコピーと一緒に軽量プレビューもコピーするので、スマホの Timeline が黒くならない

**素材ページでも選別モードと同じように素材を編集できる**
クリップを選ぶと開く詳細欄で、寄り・位置（切り出し）と使える区間（トリミング）を
選別モードとまったく同じ操作で決められます。中身は共通の部品なので、どちらで直しても結果は同じです。

- 「枠を大きく」「区間だけ再生」「IN=いま／OUT=いま」「区間を消す」を両方に用意
- 映像が読めないときの案内（クラウドでの軽量プレビュー作成）も両方に出る

**元に戻す（取り消し・やり直し）**
`catalog.json` の編集を 1 手ずつ戻せます。素材ページの上部（↶ ↷）、詳細シートの下端、
選別モードの見出しから押せます。`Ctrl+Z` / `Ctrl+Y`、保存は `Ctrl+S`。
ドラッグや文字入力のような連続した操作は 1 手にまとまります。

**選別モード・詳細シートから直接保存できる**
どちらにも「保存」を置きました（未保存なら `*` が付きます）。判定や切り出しをしたあと、
画面の上まで戻らなくても書き込めます。

**スマホでタイムラインを指で操作できるようにした**
2 本指でつまむと拡大・縮小、そのまま動かすと横移動します（つまんだ位置の時刻は動きません）。
拡大は ＋ / − ボタンでも変えられます（細いスライダーは指では狙えないので引っ込めました）。
段の厚み・掴む場所も指の幅に合わせて大きくしました。

**完成品をその場で端末に保存できるようにした**
Render の「合成と納品」に、できあがった動画（`out/final_narration.mp4`）の確認と持ち出しを置きました。

- **ダウンロード** … URL に `?download=1` を付けて、再生ではなく保存として返します
- **共有・保存** … スマホの共有シートに渡します（写真アプリや Instagram に直接送れます）

### 直したもの

- **スマホで画面を開き直しても、PC で直したキャプション・構成が反映されないことがあった** —
  変更の通知（SSE）は「繋がっている間」の変化しか流れません。スマホは画面を閉じるたびに切れるので、
  切れていた間に PC が書き戻したものを取りこぼし、案件を開き直すまで古いままでした。
  **繋ぎ直したときに、開いている案件のファイルも読み直す**ようにしました（未保存の編集はそのまま残します）。
  すぐ出したいときは上部バーの「⟳ 最新に」を押してください
- **トライアルが 80 秒レンダーしたあと「mix に失敗 (exit 1)」で落ちていた** — トライアルが
  自分で作る音声は**フック区間の 1 本だけ**で、残りのブロックの wav は先に「音声を生成」で
  作っておく必要があります。作っていない案件でも止めずにレンダーを始めてしまい、
  最後の合成で初めて落ちて、しかも理由が exit コードしか出ませんでした。
  **レンダーを始める前に**ナレーション音声と効果音ファイルが揃っているか確かめ、
  足りなければ「ナレーション音声が無いブロックがあります: … →「音声を生成」を実行してください」と
  日本語で止めます（二次活用版も同じ）。画面のほうも、音声が揃うまでトライアルのボタンを
  押せないようにして理由を出します
- **スマホで「AI ▾ 」の吹き出しが画面の外に出ていた** — ボタンの位置を基準に開く作りで、
  折り返したツールバーの右寄りにボタンが来ると収まりませんでした。狭い画面では
  画面中央のダイアログにして、後ろを覆い、「閉じる」も付けました（下端に貼り付けると
  項目がホームバーの際まで下がって押しにくく、下のほうの説明文も見落とします）
- **スマホでタイムラインの全体を見られなかった** — 拡大率の下限が 24px/秒で固定されていて、
  45 秒の動画は画面 3.5 枚ぶんありました。ピンチで縮めても「全体」を押してもそこで頭打ちです。
  下限を「いちばん狭い端末でもいちばん長い型（50 秒）が収まる」値に下げました
- **2 本指のピンチがブラウザ自身の拡大に取られることがあった** — タイムラインの中に
  指の動きを受けていない隙間が残っていました
- **mix が done なのに「out/final_narration.mp4 がありません」と出ていた**（クラウド）—
  SSE は一定時間で切れて繋ぎ直しますが、その繋ぎ目に終わったジョブの通知が届かず、
  案件の状態（out/ の有無）が古いままでした。繋ぎ直しと Render を開いたときに取り直します
- **カットを選んだときのシートで右側が空いていた** — 入力欄が小さいのに 1 行 1 個で
  積んでいたのを、入るだけ横に並べて幅を使い切るようにしました。切り出しの枠も広げています
- **詳細欄の映像が枠の中で縮んで、下に黒帯が出ていた** — 切り出し枠の高さと
  `max-height` が二重に掛かっていました。あわせて、枠の高さが割合で解決されない
  ブラウザ（iOS Safari）でも必ず埋まるように、映像を枠へ貼り付ける形にしました
- **スマホの詳細シートでコマ送り（1 秒ごとのコマ）が消えていた** — シートの中身が
  高くなったとき、`overflow` を持つ要素だけが潰れる側に回っていました
- **スマホで詳細シート・Brief の入力欄がはみ出していた** — 使える区間の行、ファクト（key: value）、
  確定フック文、slug、長い選択肢を持つ選択欄が画面外に出ていたのを、折り返す・縮むようにしました
- **チェックボックスが文字の下に出ていた** — 文字の横（行の右端）に置くようにしました
- **別のクリップに移っても slug の下書きが残っていた** — 前のクリップ名のまま
  「リネームして適用」を押せてしまう状態でした

---

## 0.2.0

### 追加

**素材の切り出し（アスペクト比を変えない拡大・位置）**
出来上がりは常に 9:16 のまま、枠の中で「どこを、どれだけ寄って見せるか」を決められます。
ピンチ・ドラッグ・ホイール・スライダーに対応。三分割の目安線と、寄ったときの実質解像度も出ます。

- 選別モードと Materials で素材ごとに決める → 構成を組むとカットへ引き継がれます
- Timeline の Inspector でカットごとにも調整できます（既にある `cuts.json` には
  「素材の切り出しを取り込む」で反映）
- 当て方はレンダー用エンジンと画面で同じ関数なので、**編集中に見えているものがそのまま書き出されます**

**選別モードで素材を編集できるようにした**
必要／不要を決めながら、その場で使える区間（トリミング）・種別・画角・被写体を直せます。
決めた区間だけを繰り返し再生して確かめられます。

**クラウドモード（Vercel の PWA ＋ 自宅 PC のワーカー）** — 任意
スマホから全機能を使えます。重い処理（ffmpeg・Remotion・Claude Code CLI）は自宅の PC が実行するので、
**AI は今までどおりログイン済みの Claude Code を使い、クラウド化による API 課金は発生しません**。
作り方は [docs/cloud.md](docs/cloud.md)。使わない場合は今までどおりローカル専用で動きます。

- ホーム画面に追加してアプリとして開ける（PWA）
- スマホで撮った動画をそのままアップロードして、PC に取り込める
- レンダーなどが終わったら通知が届く（任意）

**顔モザイク（deface）と選別モード**
店員さんや他のお客さんの顔にモザイクをかけられます。素材を 1 本ずつ見て必要／不要を素早く判定できます。

**版と更新**
Settings の「版と更新」で更新の有無が分かり、そのまま更新できます。`npm run update` でも同じことができます。

### 直したもの

- **スマホで動画が再生できなかった** — `playsInline` が無く、iOS ではインライン再生そのものが
  できていませんでした（Service Worker が動画を取り置いて再生を壊す問題も併せて修正）
- **スマホのレイアウト** — 下部ナビ、選んだクリップ／カットの詳細を手元のシートに出す、
  横にはみ出していた固定幅レイアウト（Render・設定・素材）を畳む
- **PC で素材のプレビューが追従しない** — 一覧を下へ辿るとプレビューが画面外に出ていました

### 変わったこと（更新後に起きること）

- **エンジン（テロップ描画）が変わりました。** 既存案件は「エンジン差分あり」になり、
  次のレンダーで自動的に揃います（すぐ揃えるなら Projects の「エンジン同期」）
- **依存パッケージが増えました。** 起動時に自動で入れ直します（少し時間がかかります）
- `catalog.json` / `cuts.json` / `settings.json` への追加はすべて任意項目なので、
  **今までのファイルはそのまま読めます**（古い版に戻しても壊れません）

---

## 0.1.0

最初の公開版。素材のカタログ化・タグ付け・構成プラン（F0〜F7）・台本からの組み立て・
Timeline 編集・検証・音声生成（Fish Audio）・レンダー（Remotion）・合成・納品。
